% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_path.R
\name{failing_columns}
\alias{failing_columns}
\title{Subset of columns whose validation fails}
\usage{
failing_columns(tbl, tol_cols, eq_cols, ref_suffix, na_equal)
}
\arguments{
\item{tbl}{Local data.frame holding the boolean validation columns (and, for
the local path, the raw and reference equality columns).}

\item{tol_cols}{Character vector of tolerance column names.}

\item{eq_cols}{Character vector of equality column names (already stripped of
key, type-mismatch and tolerance columns).}

\item{ref_suffix}{Suffix identifying reference columns.}

\item{na_equal}{Logical; whether NA equals NA for equality columns.}
}
\value{
A list with \code{tol} and \code{eq} character vectors of failing
column names.
}
\description{
Used on the failure path to build pointblank steps only for columns that
actually fail. Columns that pass produce empty data extracts, so omitting
their steps leaves \code{get_data_extracts()} unchanged while avoiding the
per-column agent overhead on the passing majority.
}
\keyword{internal}
