% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_path.R
\name{all_validations_pass}
\alias{all_validations_pass}
\title{Fast global pass/fail without building the per-column pointblank agent}
\usage{
all_validations_pass(tbl, tol_cols, eq_cols, ref_suffix, na_equal)
}
\arguments{
\item{tbl}{Local data.frame holding the boolean validation columns (and, for
the local path, the raw and reference equality columns).}

\item{tol_cols}{Character vector of tolerance column names.}

\item{eq_cols}{Character vector of equality column names (already stripped of
key, type-mismatch and tolerance columns).}

\item{ref_suffix}{Suffix identifying reference columns.}

\item{na_equal}{Logical; whether NA equals NA for equality columns.}
}
\value{
\code{TRUE} iff every tolerance and equality check passes.
}
\description{
The verdict produced by \code{compare_datasets_from_yaml} is entirely
determined by boolean columns already computed in a single vectorised pass
(\code{<col>__ok} for tolerance columns, \code{<col>__eq} for equality
columns on the lazy path). pointblank then re-checks those booleans through
one validation step per column, which dominates runtime on wide tables. When
every check passes there is nothing to extract, so the agent can be skipped.
}
\keyword{internal}
